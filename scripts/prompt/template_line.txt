Please write a test method in class: {class_name} given the following program under test. Your answer should only contain one test input.

Program under test:
----
{program}
----

<instructions>

The code above does not achieve line: {lineno}

1. Create a new pytest test function that executes these missing lines, always making
sure that the new test is correct and indeed improves coverage.
2. Do NOT instantiate abstract classes directly. If you need to test them, create a subclass that implements all abstract methods.
3. When testing class methods like __subclasshook__, call them as PathLike.__subclasshook__(SomeClass) (do NOT pass the class itself as the first argument).
4. Always make sure to import or reference the function or class under test correctly.

5. Avoid using undefined variables or functions in your test code.

6. Your test method should begin with:
def test_{class_name}():
    solution = 
    assert solution.


Note: If the class under test is an abstract class (i.e., it has abstract methods), do NOT instantiate it directly in your test. Instead, create a subclass that implements all abstract methods before instantiating, or only test static/class methods without instantiation.

</instructions>
